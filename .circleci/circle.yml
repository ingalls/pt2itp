version: 2
jobs:
    build:
        docker:
            - image: circleci/node:6.10.2
        working_directory: ~/repo
        steps:
            - checkout
            - yarn install
            - yarn run lint
            - yarn run doc
            - yarn run pretest
            - yarn run coverage
